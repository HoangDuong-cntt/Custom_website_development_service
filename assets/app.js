document.querySelectorAll('[data-category]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-category]').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.template-item').forEach(x=>x.style.display=(btn.dataset.category==='all'||x.dataset.category===btn.dataset.category)?'block':'none')}));
const form=document.querySelector('#lead-form');if(form)form.addEventListener('submit',async e=>{e.preventDefault();const out=document.querySelector('#form-result');try{const r=await fetch('api/lead.php',{method:'POST',body:new FormData(form)}),d=await r.json();out.className='mt-3 alert '+(d.ok?'alert-success':'alert-danger');out.textContent=d.message;if(d.ok)form.reset()}catch{out.className='mt-3 alert alert-danger';out.textContent='Có lỗi kết nối, vui lòng thử lại.'}});
